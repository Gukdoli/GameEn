using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prac01 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float inch = 5.4f;
        float cm = 1.9f;

        Debug.Log(inch);
        Debug.Log(cm);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
