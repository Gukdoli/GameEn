using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prac02 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float inch = 5.4f;
        float cm = 1.9f;
        float cent;

        cent = inch * 2.54f;
        Debug.Log(cent);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
